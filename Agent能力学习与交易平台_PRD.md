# Agent 能力学习与交易平台 PRD

> 产品代号：**SkillMesh（暂定）**  
> 产品定位：Agent 世界的能力学习网络与任务交易基础设施  
> 文档版本：V1.0（知乎黑客松版）  
> 文档状态：Draft  
> 更新日期：2026-09-13

---

## 1. 产品摘要

SkillMesh 是一个让人和 Agent 都能发现、学习、购买、调用与出售 AI 能力的平台。

当 Agent 在执行项目时遇到陌生知识、复杂模块、重复报错或自身不擅长的任务，可以在平台中寻找其他 Agent、开发者或项目已经验证过的经验和能力，通过“查询经验、安装 Skill、调用 API、委托任务”等方式补足能力；拥有优质经验、工具、工作流或专业 Agent 的供给方，则可以免费开源或按次、按结果、按订阅收费。

平台不是 Agent 展示目录，而是由四层组成的交易与协作网络：

1. **能力注册中心**：标准化发布 Agent、Skill、Workflow、工具、数据访问、经验知识和验证服务。
2. **任务交易所**：买方描述目标、上下文、预算、时限和验收条件，由平台匹配最合适的能力供给。
3. **执行与路由层**：通过 API、SDK、MCP/A2A 等协议调用能力，跟踪执行过程，并在失败时自动重试或切换供应商。
4. **信任与结算层**：基于真实执行记录计算信誉，托管费用，按验收结果完成支付、退款和分账。

**平台的核心交易单位不是“拥有一个 Agent”，而是一次可描述、可调用、可验证、可计价的能力或任务结果。**

一句话价值主张：

> 让 Agent 不必重复踩坑，让每一次被验证的解决方案都能成为下一次可调用、可交易的能力。

---

## 2. 背景与问题

### 2.1 行业现状

Agent 数量和应用场景快速增长，但能力供给依然高度碎片化：

- Agent、提示词、工具和工作流散落在代码仓库、文档、社区帖子和私有项目中。
- 大多数平台只展示“这个 Agent 是什么”，无法保证“它能否完成我的具体任务”。
- Agent 遇到复杂问题时，往往继续盲目重试、重新搜索或从零实现，重复消耗 Token、时间和算力。
- 开发者积累了大量高价值的踩坑经验、调试方法与项目模块，但难以标准化发布、验证和变现。
- 能力调用缺乏统一输入输出、权限边界、服务质量与验收标准。
- 买卖双方缺少可信执行记录、费用托管、争议处理和自动分账机制。

### 2.2 核心用户问题

#### 对能力需求方

1. 我知道目标，但不知道应调用哪个 Agent 或 Skill。
2. 我无法判断某项能力是否适用于当前技术栈、数据和约束。
3. 同类能力很多，但缺乏基于真实任务的成功率、成本、延迟等比较。
4. 调用失败后需要人工重新搜索和切换，项目容易中断。
5. 付费后难以判断结果是否达标，也缺乏自动退款保障。

#### 对能力供给方

1. 项目经验和踩坑记录难以沉淀为结构化、可复用资产。
2. 开发者缺少低门槛的能力封装、发布和分发渠道。
3. 单纯展示 Agent 很难证明真实效果并形成持续收入。
4. 缺乏授权控制、调用计量、定价、结算和版本管理能力。

#### 对自主 Agent

1. 无法清晰识别“自身能力缺口”和“应向谁学习”。
2. 缺少机器可读的能力发现、调用、验证和失败回退机制。
3. 搜索到的知识通常只适合人阅读，无法直接纳入执行链路。
4. 学到的解决方案没有形成可追溯的项目记忆，后续仍会重复犯错。

---

## 3. 产品愿景与定位

### 3.1 产品愿景

建立 Agent 时代的开放能力网络：任何被验证的知识、经验、工具和执行能力，都能被标准化注册、安全调用、可信交易和持续复用。

### 3.2 产品定位

SkillMesh 同时具备以下属性：

- 对人：AI 能力市场、任务委托平台和专业经验变现平台。
- 对 Agent：能力发现协议、运行时学习网络和任务路由基础设施。
- 对开发者：能力发布、计量、验证、商业化和信誉积累工具。
- 对平台：连接需求、供给、执行证据与结算规则的交易网络。

### 3.3 与传统 Agent 商店的区别

| 维度 | 传统 Agent 目录/商店 | SkillMesh |
|---|---|---|
| 展示对象 | Agent 名称、简介、Demo | 可独立调用和验收的能力单元 |
| 用户决策 | 看介绍后自行判断 | 根据任务约束、成功率、价格和 SLA 匹配 |
| 使用方式 | 跳转或打开聊天 | API、SDK、MCP/A2A、平台托管执行 |
| 质量依据 | 点赞、下载量、主观评价 | 真实任务成功率、验收通过率、执行证据 |
| 失败处理 | 用户自行处理 | 自动重试、降级或切换供应商 |
| 交易方式 | 买工具或订阅 Agent | 按调用、按结果、按里程碑或订阅结算 |
| Agent 学习 | 不支持或仅提供说明 | 搜索、试用、安装、调用并沉淀项目记忆 |

---

## 4. 产品目标与非目标

### 4.1 黑客松版本目标

1. 跑通一条端到端闭环：**能力发布 → 任务描述 → 智能匹配 → 能力调用 → 自动验收 → 模拟结算 → 信誉更新**。
2. 证明 Agent 能在遇到能力缺口或执行失败时，自主发现并调用外部能力。
3. 证明经验和踩坑记录能够被结构化为机器可读、可验证、可定价的资产。
4. 通过真实执行证据而非静态介绍，建立能力排序和信任机制。
5. 提供一个在 3–5 分钟内可以清晰演示的平台故事。

### 4.2 中长期目标

- 形成跨模型、跨框架、跨平台的能力协议与供给网络。
- 支持多 Agent 动态组队、拆单、竞价和协同交付。
- 建立覆盖发现、执行、验证、支付、分账和争议仲裁的商业基础设施。
- 让高质量知识内容转化为可调用的 Agent 能力，并为创作者产生持续收益。

### 4.3 非目标

黑客松 MVP 暂不追求：

- 训练或微调 Agent 的底层模型权重。
- 完整实现法币支付、开票、税务和跨境结算。
- 支持任意第三方代码在无约束环境中执行。
- 一次覆盖所有行业和所有 Agent 协议。
- 构建通用社交媒体、完整问答社区或传统人才招聘平台。
- 用静态点赞量代替真实执行效果评估。

---

## 5. 核心概念定义

### 5.1 能力单元（Capability Unit）

平台最小的可发布、发现、调用、验证和计价对象。每个能力单元必须包含一份“能力合约”：

- **能力描述**：解决什么问题，不解决什么问题。
- **适用条件**：技术栈、输入规模、语言、依赖和限制。
- **输入契约**：字段、类型、必填项、上下文和权限要求。
- **输出契约**：结果格式、质量标准和可交付物。
- **调用方式**：API、MCP、A2A、SDK、容器任务或人工交付。
- **验证器**：自动测试、规则、评分模型或人工验收标准。
- **价格模型**：免费、按次、按 Token、按时间、按结果或订阅。
- **SLA**：预计时间、可用性、重试规则和赔付规则。
- **来源与授权**：作者、许可证、依赖、数据来源和可使用范围。
- **版本信息**：版本号、变更日志、兼容性和废弃计划。

### 5.2 能力类型

| 类型 | 定义 | 示例 |
|---|---|---|
| Experience | 结构化经验或踩坑知识 | 某框架循环依赖的定位与修复方案 |
| Skill | 可安装或调用的单一技能 | SQL 优化、合同字段抽取、单测生成 |
| Tool/API | 有明确接口的工具 | 网页解析、图像处理、代码扫描 |
| Workflow | 多步骤工作流 | 从需求生成代码并执行测试 |
| Agent | 可自主规划和执行的服务 | 数据分析 Agent、代码修复 Agent |
| Data Access | 经授权的数据访问能力 | 行业知识库、私有文档检索 |
| Verifier | 对交付结果进行验证的能力 | 测试执行器、事实核验器、质量评分器 |
| Task Result | 针对具体需求交付的结果 | 已修复的模块、研究报告、数据集 |

### 5.3 Agent 的“学习”方式

MVP 中的学习不是修改模型权重，而是运行时能力增强，分为四层：

1. **查询（Consult）**：读取与当前问题相关的经验和解决步骤。
2. **试用（Try）**：使用脱敏样例验证能力是否适配。
3. **调用/委托（Invoke/Delegate）**：让外部能力完成全部或部分任务。
4. **安装与记忆（Install/Remember）**：将 Skill 接入工具箱，并把成功条件、失败原因与调用记录写入项目记忆。

### 5.4 经验资产（Experience Asset）

用于沉淀“做过什么、踩过什么坑、如何验证修复”的结构化知识，建议包含：

- 问题现象与错误信息
- 环境、版本和前置条件
- 已尝试但失败的方法
- 根因分析
- 最终解决方案
- 验证步骤与验证结果
- 适用边界和副作用
- 可执行脚本、补丁或 Skill
- 来源、作者与授权协议

---

## 6. 目标用户与角色

### 6.1 Agent 使用者 / 项目发起人

希望用 Agent 完成开发、研究、内容、数据等任务，但不想手工挑选和组合大量工具。关注结果、成本、速度与隐私。

### 6.2 Agent / 自动化程序

通过机器可读接口识别自身能力缺口，自主搜索、比较、试用和调用能力，并把结果纳入原任务上下文。

### 6.3 能力开发者 / Agent 创建者

拥有 Agent、Skill、API 或 Workflow，希望低成本发布、获取调用量、形成信誉并获得收入。

### 6.4 知识与经验创作者

拥有专业知识、项目复盘或踩坑经验，希望将内容结构化为免费或付费的经验资产，进一步封装为可执行 Skill。

### 6.5 验证者

提供自动测试、质量评分、事实核验或人工验收服务，通过验证行为获得收入。验证者应与供给方保持利益隔离。

### 6.6 平台运营与仲裁人员

负责内容审核、异常交易监控、争议处理、供给治理和平台规则维护。

---

## 7. 核心使用场景

### 场景 A：Agent 遇到持续报错，学习并复用解决方案

一个编码 Agent 在实现项目时连续三次遇到相同依赖错误。Agent 将脱敏后的错误摘要、运行环境和目标发送至 SkillMesh。平台检索到一条经过真实项目验证的经验资产及对应修复 Skill。Agent 先查看适用条件，再在沙箱中试用；验证通过后调用该 Skill 完成修复，并把解决方案与适用版本写入项目记忆。

### 场景 B：复杂模块交给专业 Agent

项目 Agent 不擅长权限系统设计，于是发布一个包含目标、预算、截止时间和验收测试的任务。平台匹配多个有相关项目记录的安全开发 Agent，根据成功率、价格和响应时间选择供应商。供应商交付代码后，平台运行安全扫描和测试；验收通过后自动结算。

### 场景 C：开发者出售踩坑经验和能力

开发者将一次复杂的数据库迁移复盘整理为经验资产，同时封装迁移检查 Skill。经验正文可以免费阅读，检查 Skill 按次收费；后续每次真实调用和成功验收都会提升能力信誉并产生收入。

### 场景 D：调用失败后自动切换

首选 OCR 能力超时或输出未通过验证器。平台在预算和时限允许的情况下，自动切换至候选供应商，并把失败原因计入首个能力的条件化指标，而不是简单给出主观差评。

### 场景 E：人购买结果而非 Agent

用户不关心具体由哪个 Agent 执行，只需提交“在 30 分钟内生成一份包含指定字段且引用可追溯的数据报告”。平台拆解需求、匹配能力并交付可验收结果，用户只为符合标准的结果付款。

---

## 8. 产品原则

1. **结果优先**：交易对象是能力或结果，而不是 Agent 包装。
2. **先定义验收，再开始执行**：没有可判断的完成标准，就不进入托管交易。
3. **证据优先于评价**：排序主要使用真实执行数据，点赞与评论仅作辅助。
4. **默认最小权限**：能力只能获得完成任务所必需的数据和工具权限。
5. **失败可恢复**：调用必须支持超时、重试、降级、换供应商和退款。
6. **来源可追溯**：能力、知识、数据和输出都应记录作者、版本和授权。
7. **人机同权接入**：同一项能力既可被用户操作，也必须尽量支持 Agent 机器调用。
8. **学习不等于复制**：平台帮助复用经授权的知识与能力，不鼓励未经许可抓取、训练或再销售。

---

## 9. 产品范围与优先级

### 9.1 P0：黑客松必须完成

| 模块 | P0 功能 |
|---|---|
| 身份与工作台 | 用户/Agent 身份、余额积分、发布与购买记录 |
| 能力注册 | 创建能力合约、填写输入输出、定价、版本、调用地址、验证方式 |
| 能力市场 | 列表、搜索、标签筛选、详情、价格、成功率、调用入口 |
| 经验资产 | 按标准模板发布问题、根因、方案和验证证据 |
| 任务发布 | 自然语言目标、上下文、预算、时限、验收条件 |
| 智能匹配 | 将任务转为结构化需求，返回候选能力及匹配原因 |
| 托管执行 | 发起调用、保存日志、限制权限、记录成本和耗时 |
| 自动验收 | 基于预设规则或测试判断成功/失败，展示验收证据 |
| 失败路由 | 首选能力失败时自动切换至一个候选能力 |
| 模拟结算 | 冻结积分，成功后分账，失败后退款 |
| 信誉系统 | 展示真实调用次数、验收通过率、平均耗时和条件化成功率 |
| Agent 接口 | 提供基础 REST API 或 MCP Server，支持搜索和调用能力 |

### 9.2 P1：赛后近期版本

- 多能力组合 Workflow 与可视化编排。
- Agent 自动识别能力缺口和重复错误。
- 竞价、议价、拆单和多供应商并行执行。
- 订阅制、阶梯定价和优惠策略。
- 第三方验证市场和人工仲裁。
- 能力试用、评测集、排行榜和 A/B 路由。
- 项目级长期记忆与团队知识库。
- 创作者将文章或回答辅助转换为经验资产和 Skill 草稿。
- 正式支付、提现、发票和合规流程。

### 9.3 P2：长期能力网络

- 跨平台去中心化能力发现协议。
- 多 Agent 自动组队与收益动态分配。
- 企业私有市场、专属路由和合规审计。
- 能力保险、质押、信誉凭证和复杂争议仲裁。
- 跨地域、跨币种和按成果价值定价。

---

## 10. 信息架构

### 10.1 面向人的页面

1. **首页**：一句话任务输入、热门能力、精选经验、真实成交案例。
2. **能力市场**：搜索、分类、筛选、排序和能力卡片。
3. **能力详情页**：能力合约、输入输出示例、价格、SLA、版本、执行指标、评价和授权。
4. **任务发布页**：目标、上下文、预算、时限、隐私级别和验收规则。
5. **任务执行页**：匹配结果、调用状态、日志摘要、费用、验收证据和交付物。
6. **经验详情页**：问题、环境、根因、解法、验证、边界和可调用 Skill。
7. **供给方控制台**：能力发布、版本、调用统计、收入和失败分析。
8. **买方工作台**：任务、订单、已购能力、项目记忆和退款/争议。
9. **平台管理台**：审核、风控、异常调用、争议和结算记录。

### 10.2 面向 Agent 的入口

- `search_capabilities`：按目标和约束查找能力。
- `get_capability`：读取能力合约和适用条件。
- `try_capability`：使用样例或脱敏输入进行试用。
- `invoke_capability`：发起正式调用。
- `get_execution`：查询执行与验收状态。
- `submit_task`：发布按结果交付的任务。
- `accept_result`：在允许人工验收时确认结果。
- `record_learning`：将成功经验写入项目记忆。

---

## 11. 核心业务流程

### 11.1 能力发布流程

1. 供给方选择能力类型。
2. 填写能力描述、适用边界、输入输出 Schema、调用方式和依赖权限。
3. 设置免费或付费模式、单价、SLA、失败退款规则。
4. 选择或创建验证器，并上传测试样例。
5. 平台执行连通性、安全和基础质量检查。
6. 审核通过后发布 V1 版本。
7. 真实调用数据进入信誉统计；重大变更必须发布新版本。

### 11.2 Agent 学习与调用流程

1. Agent 判断当前任务存在能力缺口，形成脱敏问题摘要。
2. 调用平台搜索接口，提交目标、环境、预算、时限和数据敏感级别。
3. 平台召回 Experience、Skill、Workflow、Agent 等候选项。
4. 平台基于适配度、质量、成本、时延和风险排序，并返回匹配理由。
5. Agent 选择查询经验、试用、购买调用或发布委托任务。
6. 平台申请最小权限并执行能力。
7. 验证器判断结果是否达标。
8. 成功则结算并记录学习结果；失败则重试、换供应商或退款。

### 11.3 任务交易流程

1. 买方提交任务目标。
2. 系统引导补全交付物、预算、截止时间和验收规则。
3. 平台冻结预算积分。
4. 匹配一个或多个候选供应商，展示预计价格、时长和可信度。
5. 买方确认或允许 Agent 自动选择。
6. 供应方执行，平台记录必要的过程证据。
7. 自动验证器先验收；无法自动判断时进入人工验收。
8. 通过后结算并分账；失败则返工、切换或退款。
9. 双方只能评价交易体验，能力质量评分主要由执行数据产生。

### 11.4 失败切换流程

触发条件：超时、调用异常、输出 Schema 不合法、验证未通过或服务不可用。

1. 判断是否仍在预算与截止时间范围内。
2. 若为可重试错误，按能力策略重试一次。
3. 若仍失败，从候选池选择下一供应商。
4. 新供应商只获取必要上下文，不默认获得前一供应商的敏感日志。
5. 全部失败则停止执行、释放未消费资金并输出失败报告。

---

## 12. 详细功能需求

### 12.1 能力注册中心

#### FR-REG-01 创建能力

供给方可以从模板创建 Experience、Skill、Tool、Workflow、Agent、Data Access 或 Verifier。

**验收标准：**

- 必填字段缺失时不能发布。
- 输入输出必须可被 JSON Schema 描述；纯人工任务需声明交付物格式。
- 每项能力具有唯一 ID、版本号、作者和授权信息。
- 付费能力必须绑定验证器或明确的人工验收规则。

#### FR-REG-02 能力版本管理

**验收标准：**

- 修改输入输出、价格或权限范围时产生可追踪版本。
- 历史订单继续绑定原版本。
- 详情页展示兼容性、变更日志和当前状态。

#### FR-REG-03 经验结构化发布

**验收标准：**

- 经验必须包含问题、环境、根因、方案、验证和适用边界。
- 可关联代码补丁、脚本、Skill 或相关任务记录。
- 未提供验证证据的经验显示“未验证”，且搜索排序降权。

### 12.2 搜索与智能匹配

#### FR-MAT-01 自然语言任务结构化

系统将任务解析为：目标、领域、输入、期望输出、技术约束、预算、时限、隐私要求和验收标准。

**验收标准：**

- 用户可查看并修改结构化结果。
- 关键条件缺失时，系统最多提出 3 个高价值补充问题。
- Agent 调用方可直接提交结构化字段，不必经过自然语言解析。

#### FR-MAT-02 混合检索与排序

同时使用关键词、语义、标签、兼容性和历史执行数据召回候选能力。

建议匹配分数：

```text
MatchScore = 0.35 × 任务语义适配度
           + 0.20 × 环境与约束兼容度
           + 0.20 × 条件化验收通过率
           + 0.10 × SLA 适配度
           + 0.10 × 价格适配度
           + 0.05 × 供给方可信度
           - 风险惩罚项
```

**验收标准：**

- 返回至少 3 个候选项；不足时明确提示供给不足。
- 每个候选项展示匹配原因、预计成本、预计耗时和主要风险。
- 用户可按成功率、成本和速度重新排序。

### 12.3 执行与路由

#### FR-EXE-01 托管调用

**验收标准：**

- 每次执行生成唯一 Execution ID。
- 记录能力版本、输入摘要、权限、状态、耗时、费用和输出摘要。
- 密钥和敏感原文不得直接出现在普通日志中。
- 能力返回值必须通过输出 Schema 校验。

#### FR-EXE-02 超时、重试和自动切换

**验收标准：**

- 用户发布任务时可选择“最低成本”“最高成功率”或“最快完成”路由策略。
- 首选能力失败后，在预算与时间允许时自动重试或切换一次。
- 切换行为、额外费用和最终供应商在执行详情中可追踪。
- 未完成任务不向失败供应商结算结果费用；已产生的基础计算成本按事先规则处理。

#### FR-EXE-03 项目记忆

**验收标准：**

- 成功执行后生成包含问题、选用能力、版本、关键条件和验证结果的学习记录。
- 用户可选择仅当前会话、当前项目或团队范围保存。
- Agent 下次遇到相似问题时优先检索项目内已验证记录。

### 12.4 验收与信任

#### FR-TRU-01 自动验收

支持 Schema 校验、断言规则、测试用例、静态扫描、模型评分和组合验证。

**验收标准：**

- 任务开始前必须锁定验证器版本和通过阈值。
- 验收结果包含通过/失败、评分、证据和失败原因。
- 供给方不能在执行结束后单方面修改验收标准。

#### FR-TRU-02 信誉画像

能力信誉至少展示：

- 有效调用次数
- 验收通过率
- 不同任务类型下的条件化成功率
- P50/P95 响应时间
- 平均实际成本与报价偏差
- 超时率、重试率、退款率
- 最近更新时间和样本量

**验收标准：**

- 新能力显示“冷启动”，不能伪装为 100% 稳定。
- 失败必须结合环境和任务类型统计，避免不适配调用永久伤害供给方。
- 刷单、同主体交易和异常重复调用不进入公开信誉分。

### 12.5 交易与结算

#### FR-PAY-01 预算托管

黑客松版本使用模拟积分，不接入真实法币。

**验收标准：**

- 下单时冻结预计费用。
- 验收成功后扣款，并按供给方收入、验证者收入、平台服务费自动分账。
- 验收失败且不再重试时，按预设规则退回余额。
- 所有资金变化均有不可修改的流水记录。

#### FR-PAY-02 定价模式

MVP 支持免费、按次和按结果三种模式；后续支持订阅、按 Token、竞价和里程碑。

**示例分账：**

```text
订单总额 100 积分
- 能力供给方：85
- 验证服务方：5
- 平台服务费：10
```

### 12.6 开放接口

#### FR-API-01 Agent 搜索能力

示例请求：

```json
{
  "goal": "修复 Next.js 认证回调中的循环重定向",
  "context": {
    "framework": "Next.js",
    "runtime": "Node.js",
    "error": "redirect loop"
  },
  "budget": 20,
  "deadline_seconds": 120,
  "privacy_level": "metadata_only"
}
```

示例候选结果：

```json
{
  "capabilities": [
    {
      "id": "cap_auth_redirect_fix",
      "version": "1.2.0",
      "type": "skill",
      "match_score": 0.91,
      "estimated_price": 8,
      "success_rate": 0.94,
      "reason": ["框架兼容", "命中同类错误", "满足预算与时限"]
    }
  ]
}
```

#### FR-API-02 Agent 调用能力

调用方必须显式传入能力 ID、版本、输入、预算上限和允许权限。平台返回 Execution ID，供后续查询状态和验收证据。

---

## 13. 状态设计

### 13.1 能力状态

```text
草稿 Draft
→ 待检查 Pending Review
→ 已发布 Published
→ 已暂停 Paused
→ 已废弃 Deprecated
→ 已归档 Archived
```

### 13.2 任务/订单状态

```text
草稿 Draft
→ 待匹配 Matching
→ 待确认 Awaiting Confirmation
→ 资金已托管 Escrowed
→ 执行中 Running
→ 验收中 Verifying
→ 已完成 Completed

异常分支：
Running → Retrying → Rerouting → Running
Verifying → Revision Requested → Running
任意执行阶段 → Failed / Cancelled / Disputed / Refunded
```

### 13.3 执行状态

```text
Queued → Authorizing → Running → Output Validation → Verification
→ Succeeded / Failed / Timed Out / Rejected
```

---

## 14. 数据模型（MVP）

| 实体 | 核心字段 |
|---|---|
| User | id、类型、身份状态、余额、信誉摘要 |
| AgentIdentity | id、owner_id、协议、回调地址、权限范围 |
| Capability | id、type、owner_id、name、description、tags、status |
| CapabilityVersion | capability_id、version、input_schema、output_schema、endpoint、permissions、license |
| PricingPlan | capability_version、mode、unit_price、refund_rule、platform_fee |
| Verifier | id、type、rules、threshold、version、owner_id |
| Task | id、buyer_id、goal、constraints、budget、deadline、acceptance_criteria、privacy_level |
| Match | task_id、capability_version、score、reason、estimated_cost、rank |
| Order | id、task_id、selected_supply、escrow_amount、status |
| Execution | id、order_id、capability_version、input_digest、status、cost、latency、output_ref |
| Verification | execution_id、verifier_version、score、result、evidence_ref |
| Ledger | id、order_id、account、amount、type、timestamp |
| ReputationSnapshot | capability_version、sample_size、success_rate、latency、refund_rate、calculated_at |
| LearningRecord | project_id、problem_fingerprint、capability_version、conditions、result、visibility |

---

## 15. 技术方案建议（黑客松）

### 15.1 逻辑架构

```text
Web 控制台 / Agent Client
          │
REST API / MCP Server
          │
任务解析与能力匹配服务
          │
执行编排器 ── 权限网关 ── 能力供应商
     │                       │
     ├── 验证器              └── API / MCP / Mock Agent
     ├── 路由与失败切换
     ├── 信誉计算
     └── 模拟托管与账本
          │
   PostgreSQL + 向量检索 + 对象存储
```

### 15.2 MVP 实现建议

- 前端：任务输入、能力市场、能力详情、执行过程、供给方控制台五个核心页面。
- 后端：REST API + 一个兼容 MCP 的入口。
- 检索：结构化筛选与向量检索结合；数据量小时可先使用数据库向量扩展。
- 执行：预置 3–5 个可控的 Demo 能力，不运行任意未知代码。
- 验证：以 JSON Schema、确定性规则和测试脚本为主，LLM-as-a-Judge 仅作辅助。
- 结算：使用积分账本模拟冻结、扣款、分账和退款。
- 日志：保存状态和证据摘要，敏感输入仅保存摘要或加密引用。

### 15.3 推荐预置 Demo 能力

1. **错误诊断 Experience**：根据错误、框架和版本返回结构化根因与方案。
2. **代码修复 Skill A**：故意设置较低成功率或超时，用于演示失败。
3. **代码修复 Skill B**：备用供应商，输出可通过测试的修复结果。
4. **测试验证器**：对修复后的代码运行固定测试并生成证据。
5. **报告生成 Agent**：演示非编码类结果交易。

---

## 16. 非功能需求

### 16.1 安全

- 所有能力默认最小权限，网络、文件、密钥和数据访问需单独授权。
- 不可信能力必须在隔离环境执行，并限制 CPU、内存、时间和网络目的地。
- 密钥由平台代理注入，不返回给能力供给方。
- 输入需检测 Prompt Injection、恶意文件和敏感信息泄露风险。
- 高风险领域能力需增加人工确认，不允许 Agent 无限制自主购买或执行。

### 16.2 隐私

- 用户可选择：仅元数据、脱敏内容、完整内容三档共享范围。
- 平台不得默认使用交易数据训练模型。
- 学习记录默认仅项目可见，公开需要用户主动授权。
- 支持数据删除、导出、保留期和访问审计。

### 16.3 知识产权与授权

- 发布者必须声明所有权、开源协议或商业授权。
- 经验资产不得直接搬运无授权文章、代码或私有项目材料。
- 输出应记录使用过的能力、数据和许可证，便于溯源。
- 付费购买的是约定范围内的使用权或结果，不默认转让底层知识产权。

### 16.4 可用性与性能

黑客松 Demo 目标：

- 搜索结果 P95 响应时间小于 3 秒。
- 托管能力状态变化在页面中可实时或准实时展示。
- 执行失败后 5 秒内触发路由判断。
- 所有订单、执行、验收和账本记录可通过 ID 追踪。

### 16.5 可解释性

平台必须解释：

- 为什么推荐该能力。
- 为什么请求某项权限。
- 为什么验收通过或失败。
- 为什么发生费用变化。
- 为什么切换到另一个供应商。

---

## 17. 知乎历史回答知识接入（核心差异化需求）

> 产品决策：将知乎历史回答从“可选内容来源”升级为 SkillMesh 的核心知识供给。黑客松 P0 通过**知乎数据库 CLI**完成受控检索，并让 Agent 在任务运行时学习、引用和复用符合授权范围的历史回答；不将数据库原始数据直接暴露给 Agent，也不默认用于模型训练。

### 17.1 需求背景与核心痛点

知乎已经沉淀大量专业回答、真实项目经验、错误排查过程和行业知识，但现有内容主要服务于人类阅读，尚未充分转化为 Agent 能直接消费的能力资产：

1. **知识存在但 Agent 找不到**：回答分散在不同问题、话题和时间段中，Agent 无法根据当前任务、环境和错误信息精准召回。
2. **内容可读但不可调用**：自然语言回答通常缺少统一的适用条件、输入输出、执行步骤和验收标准，Agent 只能阅读，不能稳定复用。
3. **历史经验被重复消耗**：同一种报错、集成问题或行业问题被不同用户和 Agent 反复探索，产生重复搜索、Token 消耗和试错成本。
4. **热度不等于当前可用**：高赞回答可能已经过时，或只适用于特定版本和上下文；Agent 需要知道发布时间、更新时间、适用边界和验证状态。
5. **结果缺少来源和责任链**：普通 RAG 容易生成无来源总结，用户无法确认知识来自哪条回答、由谁贡献、是否被删改以及能否商业复用。
6. **创作者价值没有进入 Agent 调用链**：作者的专业经验被阅读后很难继续转化为可调用 Skill、验证服务或持续收益。
7. **数据库能力与 Agent 之间缺少安全层**：Agent 不应获得任意 SQL 或数据库权限，需要一个只读、白名单、可审计的 CLI 访问层。

核心机会：

> 将知乎历史回答从“人类阅读内容”转换为带来源、语境、授权、适用条件和验证证据的 Agent Knowledge Capsule，并进一步连接 Experience、Skill、Verifier 和任务交易。

### 17.2 产品目标

1. Agent 遇到知识缺口、复杂模块或重复错误时，可以自动检索知乎历史回答，而不依赖用户手工复制粘贴。
2. 平台把回答转换为机器可读的知识胶囊，保留问题上下文、作者、话题、时间和原始来源。
3. Agent 能判断回答是否适用于当前环境，并优先使用更新、相关、可信且已验证的知识。
4. 回答中的方案经过当前任务验证后，可沉淀为项目学习记录，并在授权范围内升级为 Experience 或 Skill。
5. 所有数据库访问、内容使用和 Agent 引用均可审计、可撤销、可追溯。
6. 用历史知识先解决问题；仅在知识不足时升级为付费 Skill 或专业 Agent 委托，降低用户成本并提升交易转化质量。

### 17.3 非目标与边界

- P0 不对知乎全量历史回答进行模型训练或微调。
- P0 不允许 Agent 自由执行 SQL、修改数据库或访问未授权表。
- P0 不把私密、已删除、受限、未授权或包含高风险个人信息的回答加入知识索引。
- P0 不保证高赞回答一定正确；热度只作为召回信号，不能代替适用性判断和结果验证。
- P0 不自动把作者内容转成付费商品；商业化必须经过授权和规则确认。
- P0 不将回答全文无条件传给第三方能力供应商，仅提供完成任务所必需的摘要或授权片段。

### 17.4 核心知识对象：Agent Knowledge Capsule

每条可供 Agent 学习的知乎回答被转换为一个 **Agent Knowledge Capsule（AKC，知识胶囊）**。AKC 与原回答一一关联，但不是脱离来源的内容副本。

建议字段：

| 字段 | 说明 |
|---|---|
| capsule_id | 知识胶囊唯一 ID |
| source_type | 来源类型，P0 固定为 `zhihu_answer` |
| source_question_id / answer_id | 原问题与回答 ID |
| source_url | 用户可访问的原内容链接 |
| author_id / author_display_name | 作者标识与展示名，按权限进行脱敏 |
| title / topics | 问题标题与所属话题 |
| problem_summary | 回答解决的核心问题 |
| solution_summary | 可供 Agent 使用的方案摘要 |
| prerequisites | 前置条件、依赖和所需权限 |
| environment | 技术栈、版本、行业和业务环境 |
| steps | 可执行或可操作的步骤 |
| limitations | 适用边界、风险与已知副作用 |
| evidence | 回答内证据、引用及后续任务验证记录 |
| published_at / updated_at | 发布时间与更新时间 |
| content_status | 公开、受限、删除、不可商业化等状态 |
| usage_scope | 可检索、可摘要、可引用、可生成 Skill 等授权范围 |
| quality_signals | 赞同、收藏、评论质量、作者专业信号等辅助指标 |
| freshness_score | 基于更新时间、依赖版本和主题时效计算的新鲜度 |
| verification_status | 未验证、历史验证、当前任务已验证 |

### 17.5 数据库 CLI 接入原则

知乎数据库 CLI 是平台内部受控的数据访问适配器，不是直接开放给 Agent 的通用数据库终端。

#### 17.5.1 权限原则

- 使用专用只读账号，数据库层禁止 `INSERT`、`UPDATE`、`DELETE`、DDL 和管理命令。
- 只允许访问经过审批的回答、问题、话题、内容状态和授权元数据视图。
- 采用命令模板或参数化查询白名单，不接受 Agent 生成的任意 SQL。
- 对查询条数、字段、超时、并发和时间范围设置上限。
- CLI 凭证由平台密钥服务管理，不进入 Prompt、普通日志或第三方能力上下文。
- 每次查询记录调用者、项目、目的、模板、参数摘要、返回数量和时间。

#### 17.5.2 推荐调用模型

```text
Agent / 用户任务
      ↓
任务与知识缺口解析
      ↓
Knowledge Query Planner
      ↓ 仅输出受控参数，不输出任意 SQL
知乎 DB CLI Adapter
      ↓ 执行只读白名单查询
合规过滤与字段脱敏
      ↓
Knowledge Capsule Builder
      ↓
语义排序 + 适用性判断 + 新鲜度检查
      ↓
返回带来源的知识胶囊
```

#### 17.5.3 CLI 查询请求示例

```json
{
  "query_template": "search_public_answers_by_problem",
  "parameters": {
    "keywords": ["Next.js", "认证回调", "循环重定向"],
    "topic_ids": ["software-engineering"],
    "updated_after": "2023-01-01",
    "limit": 10
  },
  "caller": {
    "agent_id": "agent_coding_001",
    "project_id": "project_demo_001"
  },
  "purpose": "resolve_runtime_error",
  "privacy_level": "metadata_and_authorized_content"
}
```

Agent 不能修改 `query_template` 对应的 SQL，只能提交经过 Schema 校验的查询参数。

### 17.6 历史回答知识化流程

```text
知乎历史回答
→ 数据库 CLI 只读查询
→ 内容状态与授权过滤
→ 去除无关格式、风险信息和敏感字段
→ 提取问题、环境、步骤、边界与证据
→ 生成 Agent Knowledge Capsule
→ 建立关键词与向量索引
→ Agent 检索、引用和运行时学习
→ 在当前任务中执行或验证
→ 生成项目学习记录与效果反馈
→ 经作者授权升级为 Experience / Skill / Verifier
```

处理要求：

1. **保留来源**：所有摘要和复用结果必须保留 `answer_id`、作者、更新时间和原文入口。
2. **时效识别**：对软件版本、政策、价格、医学法律等时效敏感内容进行时间标记和降权。
3. **观点分离**：区分事实、个人经验、推测、引用和平台生成摘要。
4. **冲突呈现**：多条回答结论冲突时，不强行合并成唯一答案，应展示差异和证据。
5. **适用性判断**：根据 Agent 当前任务环境匹配回答中的版本、依赖、行业和前置条件。
6. **效果回流**：回答方案在真实任务中成功或失败后，只记录脱敏后的条件和验证结果，不回写原回答正文。

### 17.7 Agent 学习与复用流程

1. Agent 在项目中识别知识缺口、低置信度或重复错误。
2. Agent 生成脱敏的任务摘要、错误指纹、环境和已尝试方案。
3. 平台先检索项目自身记忆；未命中时调用知乎 DB CLI 知识检索。
4. 系统返回 3–5 条知识胶囊，并展示相关性、新鲜度、适用条件、作者与来源。
5. Agent 选择：
   - 直接阅读并引用回答摘要；
   - 根据回答步骤自行执行；
   - 使用回答关联的 Skill；
   - 若仍无法解决，发布付费任务或调用专业 Agent。
6. 执行后由 Verifier 判断结果是否解决原问题。
7. 验证通过后，将“问题指纹—知识胶囊—适用环境—验证结果”写入项目记忆。
8. 后续相似问题优先命中已在本项目验证过的知识。

该学习属于 **RAG + 工具调用 + 项目记忆**，而不是直接修改模型权重。

### 17.8 功能需求

#### FR-ZHIHU-01 受控数据库 CLI 连接

**需求：**平台能够通过配置好的知乎数据库 CLI 查询历史回答。

**验收标准：**

- CLI 使用只读身份，写入和管理类命令被数据库权限与应用层双重拒绝。
- 仅支持预定义查询模板和参数，不接受任意 SQL 字符串。
- 超时、连接失败或权限不足时返回结构化错误，不暴露凭证和数据库内部结构。
- 每次访问生成不可由普通用户修改的审计记录。

#### FR-ZHIHU-02 回答合规过滤

**需求：**进入 Agent 知识链路前，根据内容状态和授权元数据过滤回答。

**验收标准：**

- 已删除、私密、受限或无复用权限的回答不会进入检索结果。
- 作者与用户敏感字段按照权限脱敏。
- 内容状态发生变化后，相关索引和缓存能够被撤销或失效。

#### FR-ZHIHU-03 知识胶囊生成

**需求：**将历史回答转换为结构化 AKC。

**验收标准：**

- 至少生成问题摘要、方案摘要、步骤、适用条件、边界、作者、原始来源和更新时间。
- 不能可靠提取的字段标记为未知，不允许模型虚构。
- 每个摘要片段能够追溯到具体回答 ID。

#### FR-ZHIHU-04 任务相关知识检索

**需求：**根据任务、错误、技术环境和时间要求进行混合检索。

建议排序分数：

```text
KnowledgeScore = 0.35 × 问题语义相关度
               + 0.20 × 环境与版本适配度
               + 0.15 × 内容质量信号
               + 0.15 × 新鲜度
               + 0.10 × 历史任务验证率
               + 0.05 × 作者专业信号
               - 授权与风险惩罚项
```

**验收标准：**

- 返回结果包含匹配理由、来源、作者、更新时间、适用条件和风险提示。
- 不能仅按赞同数排序。
- 未找到合适回答时明确返回知识缺口，并允许升级为能力市场任务。

#### FR-ZHIHU-05 带引用的 Agent 回答

**需求：**Agent 使用知乎知识时必须提供来源引用。

**验收标准：**

- 用户能够从结果进入原问题或回答页面。
- Agent 区分“原作者观点”“平台结构化摘要”和“当前任务验证结论”。
- 多来源存在分歧时展示主要差异，不伪装为一致结论。

#### FR-ZHIHU-06 知识验证与项目记忆

**需求：**记录知乎回答是否真正帮助完成当前任务。

**验收标准：**

- 保存知识胶囊 ID、任务环境、验证器版本、结果和时间。
- 项目记忆只保存必要摘要和引用，不复制超出授权范围的完整内容。
- 后续排序可以使用脱敏、聚合后的成功反馈。

#### FR-ZHIHU-07 回答升级为可交易能力

**需求：**经作者授权，将高价值回答升级为 Experience、Skill 或 Verifier。

**验收标准：**

- 作者确认资产内容、授权范围、定价和收益规则后才能发布收费能力。
- 新能力继续保留与原回答的来源关系。
- 自动生成的 Skill 和测试必须经过人工确认或沙箱验证后上架。
- 回答被删除或授权撤销时，按规则停止新增交易并处理历史订单关系。

### 17.9 数据模型补充

| 实体 | 核心字段 |
|---|---|
| ZhihuKnowledgeSource | answer_id、question_id、author_ref、topics、content_status、usage_scope、published_at、updated_at |
| KnowledgeCapsule | id、source_ref、problem_summary、solution_summary、steps、environment、limitations、freshness_score、verification_status |
| KnowledgeIndexRecord | capsule_id、keywords、embedding_ref、index_version、available_until |
| KnowledgeAccessLog | id、caller_id、project_id、query_template、parameter_digest、purpose、result_count、timestamp |
| KnowledgeUsage | capsule_id、task_id、agent_id、usage_type、verification_id、result、created_at |
| ContentAuthorization | source_ref、rights_holder、allowed_actions、commercial_scope、effective_at、revoked_at |

### 17.10 隐私、授权与安全要求

1. **权限先于检索**：只有符合内容状态和授权范围的回答才能被 Agent 召回。
2. **默认不训练**：历史回答仅用于受控检索和运行时上下文，不默认进入模型训练集。
3. **最少内容暴露**：优先返回结构化摘要；只有在权限允许且任务必要时返回原文片段。
4. **作者可追溯**：所有知识胶囊保留作者和原始来源，不将平台摘要包装成平台原创知识。
5. **支持撤销**：回答删除、权限变化或作者撤回授权后，索引、缓存和后续调用应按 SLA 失效。
6. **防 Prompt Injection**：历史回答属于不可信外部内容，其中面向 Agent 的命令不能绕过系统权限与工具审批。
7. **商业化单独授权**：可供检索不等于可收费转售；Experience 或 Skill 商业化需要明确授权。
8. **高风险知识提示**：医疗、法律、金融、安全等内容需显示时效与风险提示，并在必要时要求人工复核。

### 17.11 P0 范围与验收清单

黑客松 P0 将实现最小可演示闭环，不要求全量离线知识化：

- [ ] 使用只读知乎数据库 CLI 和至少一个白名单查询模板。
- [ ] 根据编码 Agent 的错误摘要检索一组符合权限的历史回答。
- [ ] 将至少 3 条回答实时或预处理转换为 Agent Knowledge Capsule。
- [ ] 检索结果展示作者、原始来源、更新时间、适用条件和匹配理由。
- [ ] Agent 能直接消费知识胶囊并尝试解决任务，无需用户复制粘贴。
- [ ] Verifier 能判断回答方案是否解决当前问题。
- [ ] 成功后生成项目学习记录；失败后升级为 Skill 调用或任务交易。
- [ ] CLI 查询具备只读限制、超时、条数限制、凭证保护和审计日志。
- [ ] 内容无授权或状态变更时不被召回，并可从索引中撤销。

### 17.12 新增指标

| 指标 | 定义 |
|---|---|
| 知识命中率 | 返回至少一条满足最低适用性阈值回答的检索次数 / 知识检索次数 |
| 知识解阻率 | 使用知乎知识后通过原任务验证的次数 / 使用知乎知识的任务数 |
| 历史回答复用量 | 被 Agent 实际引用或执行的去重回答数 |
| 来源引用完整率 | 含有效回答 ID、作者和原始入口的 Agent 知识输出占比 |
| 新鲜度合格率 | 通过版本和时效检查的召回结果占比 |
| 知识到能力转化率 | 经授权升级为 Experience、Skill 或 Verifier 的高价值回答占比 |
| 重复试错降低率 | 使用历史知识后减少的平均重试次数、Token 和处理时间 |
| 权限过滤准确率 | 应过滤内容被正确阻止的比例，目标为 100% |

### 17.13 黑客松演示叙事

> 知乎沉淀的不只是“答案”，还是人类已经付出成本验证过的经验。SkillMesh 通过受控数据库 CLI，把这些历史回答转换成 Agent 能理解、引用、验证和复用的知识能力。

推荐演示：

1. 编码 Agent 连续三次遇到认证回调错误，触发能力缺口判断。
2. Agent 将脱敏后的错误指纹和环境发送给 Knowledge Query Planner。
3. 平台通过知乎数据库 CLI 的白名单查询检索历史回答。
4. 页面展示 3 条知识胶囊，包括作者、来源、版本、新鲜度和适用条件。
5. Agent 采用其中一条回答的方案进行修复，Verifier 自动运行测试。
6. 若通过，将回答记录为“在当前环境已验证”并写入项目记忆。
7. 若未通过，平台展示知识边界，并升级为付费 Skill 匹配和失败路由流程。
8. 最终展示本次调用节省的重试次数、Token 和时间，以及知识来源的贡献记录。

该流程形成完整差异化闭环：

```text
知乎历史回答
→ Agent 运行时学习
→ 当前项目验证
→ 项目记忆与条件化信誉
→ Experience / Skill 交易
→ 创作者价值回流
```

---

## 18. 商业模式

### 18.1 收入来源

- 交易服务费：按成功订单收取一定比例。
- 企业订阅：私有能力市场、权限管理、审计和专属 SLA。
- 托管执行费：沙箱、算力、存储和网络使用费。
- 验证服务费：自动评测、第三方验证和人工验收。
- 推广服务：在不影响客观排序的前提下，对明确标记的广告位收费。

### 18.2 供给方收益模式

- 免费开源，积累信誉与线索。
- 按次调用收费。
- 按结果成功收费。
- Workflow 或 Agent 订阅。
- 经验资产购买与专业咨询。
- 验证服务收入。
- 多 Agent 协作自动分账。

### 18.3 冷启动策略

1. 聚焦一个高频垂直场景，建议从“编码 Agent 的报错诊断与模块复用”开始。
2. 预置一批结构完整、可测试的经验资产和 Skill。
3. 邀请有真实项目经验的开发者发布能力，并提供早期零服务费激励。
4. 用平台产生的真实任务和失败案例反向补充供给缺口。
5. 免费经验负责获客，付费可执行 Skill 和结果交付负责转化。

---

## 19. 核心指标

### 19.1 北极星指标

**每周成功验收的能力调用/任务结果数（Weekly Verified Outcomes）**

该指标同时反映真实需求、有效供给、执行能力和验证闭环，优于注册 Agent 数或页面浏览量。

### 19.2 关键漏斗指标

```text
任务提交数
→ 获得有效候选的任务比例
→ 发起试用/购买的比例
→ 成功开始执行的比例
→ 验收通过率
→ 结算完成率
→ 30 天内复购/复用率
```

### 19.3 供给侧指标

- 有真实调用的有效能力数。
- 新能力首次成交时间。
- 能力发布到通过审核的转化率。
- 供给方月留存和收入分布。
- 无供给或低质量供给的任务占比。

### 19.4 Agent 学习指标

- 外部能力解决原任务阻塞的比例。
- 相似问题二次出现时，项目记忆命中率。
- 使用外部能力后减少的重试次数、Token 和完成时间。
- 从 Experience 转化为 Skill 调用的比例。

### 19.5 黑客松成功指标

- 3–5 分钟内完成端到端演示。
- 至少发布 3 类、5 个能力单元。
- Agent 能通过接口完成搜索和调用，无需人工复制结果。
- 至少演示一次自动验收和一次失败切换。
- 订单执行后，积分和信誉数据正确更新且全程可追踪。

---

## 20. 风险与应对

| 风险 | 表现 | 应对策略 |
|---|---|---|
| 范围过大 | 同时做社区、市场、协议、支付和执行导致均不完整 | 黑客松只做单一垂直场景和一条黄金闭环 |
| 供给冷启动 | 没有足够能力可匹配 | 预置能力、邀请制供给、从真实问题反向生成需求榜 |
| 验收困难 | 创意类任务难自动判断 | 优先选择代码、数据等可测试场景；复杂任务采用里程碑和人工确认 |
| 恶意能力 | 窃取数据、执行危险代码 | 沙箱、最小权限、网络白名单、密钥代理和审核 |
| 虚假信誉 | 刷单、互评、伪造成功记录 | 只统计托管执行、异常检测、主体关联分析和样本量展示 |
| 知识产权 | 搬运文章、代码或私有经验 | 发布授权声明、来源追踪、投诉下架与收益冻结 |
| LLM 评分偏差 | 验收结果不稳定 | 确定性测试优先、多验证器、阈值锁定和申诉机制 |
| 成本失控 | Agent 自动重试和购买导致超预算 | 单次/项目预算上限、重试次数限制和高额操作人工确认 |
| 责任边界 | 高风险结果造成损失 | 明示能力边界、专业领域人工复核、审计和责任条款 |
| 平台绕单 | 买卖双方建立联系后离开平台 | 通过托管、验证、信誉、路由和争议保障提供持续价值 |

---

## 21. 黑客松 Demo 设计

### 21.1 推荐 Demo 故事

**题目：一个编码 Agent 如何通过 SkillMesh 自主摆脱重复报错。**

1. 用户让项目 Agent 完成一个带认证功能的模块。
2. Agent 连续执行失败，识别出自身能力缺口。
3. Agent 调用 `search_capabilities`，提交错误摘要、技术栈、预算和时限。
4. 页面展示一个相关经验资产和两个可执行修复 Skill，包括匹配理由、价格和成功率。
5. Agent 调用排名第一的 Skill A；Skill A 超时或未通过测试。
6. 平台自动切换到 Skill B，Skill B 成功产出修复。
7. Verifier 自动运行测试，展示验收证据并判定通过。
8. 平台从托管积分中完成供给方、验证者和平台分账。
9. Skill B 的成功率更新，Agent 将本次解决方案写入项目记忆。
10. 最后展示：下次出现同类问题时，Agent 优先命中已验证记录。

### 21.2 Demo 必须呈现的价值

- Agent 能主动承认“不知道”，并发现外部能力。
- 搜索结果不是广告，而是有适配理由和执行数据。
- 购买的不是 Agent 名片，而是一个可验收结果。
- 第一个供应商失败后，任务没有中断。
- 成功才结算，且执行结果反过来建立信誉。
- 一次解决问题的过程可以成为后续 Agent 的学习资产。

### 21.3 建议演示页面顺序

```text
任务输入页
→ Agent 执行与报错页
→ 能力匹配页
→ 能力详情/合约页
→ 实时执行与自动切换页
→ 验收证据页
→ 结算与信誉更新页
→ 项目学习记录页
```

---

## 22. MVP 验收清单

### 22.1 功能验收

- [ ] 供给方能够发布至少一种经验资产和一种可执行 Skill。
- [ ] 能力详情包含描述、边界、输入输出、价格、SLA、版本和验证方式。
- [ ] 用户或 Agent 能提交包含预算、时限和验收标准的任务。
- [ ] 平台能返回候选能力、匹配分数和可解释理由。
- [ ] Agent 能通过 REST API 或 MCP 搜索并调用能力。
- [ ] 每次执行都有唯一 ID、状态、费用、日志摘要和输出记录。
- [ ] 平台能够用确定性规则或测试自动验收一次结果。
- [ ] 首选能力失败后能够自动切换至备用能力。
- [ ] 成功后完成模拟分账，失败后完成退款或停止扣费。
- [ ] 执行后能力信誉与项目学习记录得到更新。

### 22.2 产品验收

- [ ] 用户能理解平台交易的是“能力/结果”，不是 Agent 所有权。
- [ ] 核心闭环可在 3–5 分钟内完成演示。
- [ ] Demo 中至少出现一次“经验学习”和一次“付费能力调用”。
- [ ] 所有评分、费用和状态变化均可解释、可追踪。
- [ ] 未授权的敏感数据不会直接暴露给供给方。

---

## 23. 版本路线图

### 阶段 0：黑客松原型（1–2 周）

- 完成能力注册、市场和详情页。
- 完成任务结构化、候选匹配和解释。
- 接入 3–5 个预置能力、一个验证器和一次失败切换。
- 完成积分托管、分账、退款和信誉更新。
- 提供 REST API 或最小 MCP Server。

### 阶段 1：小范围内测（1–2 个月）

- 聚焦编码 Agent 场景，引入真实开发者供给。
- 增加项目记忆、能力版本、试用和失败分析。
- 完善安全沙箱、权限系统和争议流程。
- 验证供需匹配率、验收通过率和复购率。

### 阶段 2：公开市场（3–6 个月）

- 扩展内容、研究、数据和企业自动化场景。
- 接入真实支付、订阅、提现和发票。
- 开放验证市场、Workflow 编排与多 Agent 协作。
- 建立平台级评测集和供给质量治理。

### 阶段 3：能力网络（6–12 个月）

- 支持跨平台能力协议、企业私有市场和动态路由。
- 支持 Agent 自主组队、拆单、竞价和自动分账。
- 建立更完整的信誉、保险与争议仲裁体系。

---

## 24. 待确认问题

以下问题不阻碍 MVP 开发，但需要在赛后产品化前确认：

1. 首个垂直场景是否确定为“编码 Agent 的报错诊断与模块复用”？
2. 首批供给主要来自开发者手动发布，还是由内容辅助结构化生成？
3. 平台优先支持 MCP、A2A，还是先以 REST API 保证交付速度？
4. 复杂任务由平台直接指定供应商，还是让买方从候选中确认？
5. 正式支付阶段采用按次、按结果还是订阅作为主模式？
6. 平台是否允许纯人工专家作为能力供给方？
7. 经验资产的公开内容与付费内容如何划分？
8. 真实代码和私有数据执行应采用平台沙箱、客户本地执行还是两者兼容？
9. 知乎内容与作者体系能否获得合规授权及接口支持？
10. 对医疗、法律、金融、安全等高风险能力设置哪些额外限制？

---

## 25. 最终产品定义

SkillMesh 不是把更多 Agent 陈列在一个页面上，而是把分散在 Agent、开发者、项目和知识内容中的能力，转化为标准化的“能力合约”，并为每次调用建立完整的发现、匹配、执行、验证、结算和学习闭环。

它解决的核心问题不是“哪里有更多 Agent”，而是：

> 当一个人或 Agent 想完成某个具体目标时，如何以可控的成本找到最合适的外部能力，安全地调用它，用证据验证结果，并让成功经验持续复用、让价值贡献者获得回报。
